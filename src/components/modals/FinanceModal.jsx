import React, { useState, useEffect } from 'react';
import { X, DollarSign, ShoppingBag, Link, Save, Loader, Trash } from 'lucide-react';
import { useApp } from '../../context/AppContext';
import { PROJECT_TYPES } from '../../config/constants';

const FinanceModal = () => {
    const { modals, toggleModal, operations, selectedItems, setSelectedItems, showToast, plans, openConfirm } = useApp();
    const [formData, setFormData] = useState({ type: 'income', title: '', amount: '', datetime: '', planId: '', memo: '', productUrl: '' });
    const [isSaving, setIsSaving] = useState(false);
    const formatAmount = (value) => { const num = value.replace(/[^0-9]/g, ''); return num ? Number(num).toLocaleString() : ''; };
    const handleAmountChange = (e) => { const raw = e.target.value.replace(/[^0-9]/g, ''); setFormData({ ...formData, amount: raw }); };
    const getCurrentDateTime = () => { const now = new Date(); return `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`; };
    useEffect(() => { if (modals.finance) { if (selectedItems.edit) { setFormData({ type: selectedItems.edit.type || 'income', title: selectedItems.edit.title || '', amount: selectedItems.edit.amount?.toString() || '', datetime: selectedItems.edit.datetime || getCurrentDateTime(), planId: selectedItems.edit.planId || '', memo: selectedItems.edit.memo || '', productUrl: selectedItems.edit.productUrl || '' }); } else { setFormData({ type: 'income', title: '', amount: '', datetime: getCurrentDateTime(), planId: '', memo: '', productUrl: '' }); } } }, [modals.finance, selectedItems.edit]);
    if (!modals.finance) return null;
    const handleSubmit = async () => { if (!formData.title || !formData.amount) { showToast('항목명과 금액을 입력해주세요.', 'error'); return; } setIsSaving(true); const saveData = { ...formData, id: selectedItems.edit?.id, amount: Number(formData.amount), category: 'finance' }; if (await operations.saveManagement(saveData, 'finance')) { showToast('저장되었습니다.'); setSelectedItems(prev => ({ ...prev, edit: null })); toggleModal('finance', false); } else { showToast('저장 실패', 'error'); } setIsSaving(false); };
    const handleDelete = () => { if (!selectedItems.edit?.id) return; openConfirm('이 항목을 삭제하시겠습니까?', async () => { if (await operations.deleteManagement(selectedItems.edit.id)) { showToast('삭제되었습니다.'); setSelectedItems(prev => ({ ...prev, edit: null })); toggleModal('finance', false); } }); };
    return (<div className="fixed inset-0 z-[100] flex items-center justify-center bg-[#3a2d20]/30 backdrop-blur-sm p-4"><div className="bg-[#faf6ef] rounded-2xl shadow-2xl w-full max-w-md overflow-hidden flex flex-col max-h-[85vh]"><div className={`p-5 flex justify-between items-center flex-shrink-0 ${formData.type === 'income' ? 'bg-[#5d7a5d]/10' : 'bg-[#9b4d4d]/10'}`}><div className="flex items-center gap-3"><div className={`p-2 rounded-xl ${formData.type === 'income' ? 'bg-[#5d7a5d]' : 'bg-[#9b4d4d]'} text-[#faf6ef]`}>{formData.type === 'income' ? <DollarSign size={20} /> : <ShoppingBag size={20} />}</div><div><h3 className="font-bold text-lg text-[#42392e]">{selectedItems.edit ? '재무 내역 수정' : '재무 내역 등록'}</h3><p className="text-xs text-[#6a5d50]">수입/지출 내역을 기록합니다</p></div></div><button onClick={() => { setSelectedItems(prev => ({ ...prev, edit: null })); toggleModal('finance', false); }} className="p-2 hover:bg-[#faf6ef]/50 rounded-full"><X size={20} className="text-[#857460]" /></button></div><div className="p-5 space-y-4 flex-1 overflow-y-auto custom-scrollbar"><div><label className="block text-xs font-bold text-[#6a5d50] mb-2 uppercase">구분</label><div className="flex gap-2"><button type="button" onClick={() => setFormData({ ...formData, type: 'income' })} className={`flex-1 py-3 rounded-xl font-bold text-sm flex items-center justify-center gap-2 transition-all ${formData.type === 'income' ? 'bg-[#5d7a5d] text-[#faf6ef] shadow-lg' : 'bg-[#f5f0e6] text-[#6a5d50] hover:bg-[#e8dcc8]'}`}><DollarSign size={16} /> 수입</button><button type="button" onClick={() => setFormData({ ...formData, type: 'expense' })} className={`flex-1 py-3 rounded-xl font-bold text-sm flex items-center justify-center gap-2 transition-all ${formData.type === 'expense' ? 'bg-[#9b4d4d] text-[#faf6ef] shadow-lg' : 'bg-[#f5f0e6] text-[#6a5d50] hover:bg-[#e8dcc8]'}`}><ShoppingBag size={16} /> 지출</button></div></div><div><label className="block text-xs font-bold text-[#6a5d50] mb-2 uppercase">항목명</label><input placeholder={formData.type === 'income' ? "예: 광고수익, 협찬, 슈퍼챗" : "예: 장비구매, 외주편집비, 교통비"} value={formData.title || ''} className="w-full p-3 bg-[#faf6ef] border border-[#d4c4ac] rounded-xl text-sm focus:outline-none focus:border-[#5d6a7a] text-[#32291e]" onChange={e => setFormData({ ...formData, title: e.target.value })} /></div>{formData.type === 'expense' && (<div><label className="block text-xs font-bold text-[#6a5d50] mb-2 uppercase">제품 구매 링크 (URL)</label><div className="flex gap-2"><div className="relative flex-1"><span className="absolute left-3 top-1/2 -translate-y-1/2 text-[#857460]"><Link size={16} /></span><input placeholder="https://..." value={formData.productUrl || ''} className="w-full p-3 pl-10 bg-[#faf6ef] border border-[#d4c4ac] rounded-xl text-sm focus:outline-none focus:border-[#5d6a7a] text-[#32291e]" onChange={e => setFormData({ ...formData, productUrl: e.target.value })} /></div></div></div>)}<div><label className="block text-xs font-bold text-[#6a5d50] mb-2 uppercase">금액 (원)</label><div className="relative"><span className="absolute left-3 top-1/2 -translate-y-1/2 text-[#857460] font-bold">₩</span><input placeholder="0" value={formatAmount(formData.amount?.toString() || '')} className="w-full p-3 pl-8 bg-[#faf6ef] border border-[#d4c4ac] rounded-xl text-sm font-mono text-right focus:outline-none focus:border-[#5d6a7a] text-[#32291e]" onChange={handleAmountChange} /></div></div><div><label className="block text-xs font-bold text-[#6a5d50] mb-2 uppercase">거래일</label><input type="date" value={formData.datetime || ''} className="w-full p-3 bg-[#faf6ef] border border-[#d4c4ac] rounded-xl text-sm focus:outline-none focus:border-[#5d6a7a] text-[#32291e]" onChange={e => setFormData({ ...formData, datetime: e.target.value })} /></div><div><label className="block text-xs font-bold text-[#6a5d50] mb-2 uppercase">연결 프로젝트 (선택)</label><select value={formData.planId || ''} onChange={e => setFormData({ ...formData, planId: e.target.value })} className="w-full p-3 bg-[#faf6ef] border border-[#d4c4ac] rounded-xl text-sm focus:outline-none focus:border-[#5d6a7a] text-[#32291e]"><option value="">프로젝트 미연결</option>{plans.map(p => (<option key={p.id} value={p.id}>[{PROJECT_TYPES[p.type || 'main']?.label}] {p.topic}</option>))}</select></div><div><label className="block text-xs font-bold text-[#6a5d50] mb-2 uppercase">메모 (선택)</label><textarea placeholder="추가 메모사항..." value={formData.memo || ''} className="w-full p-3 bg-[#faf6ef] border border-[#d4c4ac] rounded-xl text-sm h-20 resize-none focus:outline-none focus:border-[#5d6a7a] text-[#32291e]" onChange={e => setFormData({ ...formData, memo: e.target.value })} /></div></div><div className="p-4 border-t border-[#e8dcc8] flex justify-between items-center bg-[#faf6ef] flex-shrink-0">{selectedItems.edit ? (<button onClick={handleDelete} className="px-4 py-2 text-[#9b4d4d] hover:bg-[#9b4d4d]/10 rounded-lg text-sm font-bold flex items-center gap-1"><Trash size={14} /> 삭제</button>) : <div />}<div className="flex gap-2"><button onClick={() => { setSelectedItems(prev => ({ ...prev, edit: null })); toggleModal('finance', false); }} className="px-4 py-2 text-[#6a5d50] hover:bg-[#e8dcc8] rounded-lg text-sm font-bold">취소</button><button onClick={handleSubmit} disabled={isSaving} className={`px-6 py-2 text-[#faf6ef] font-bold rounded-lg text-sm flex items-center gap-2 disabled:opacity-50 ${formData.type === 'income' ? 'bg-[#5d7a5d] hover:bg-emerald-600' : 'bg-[#9b4d4d] hover:bg-rose-600'}`}>{isSaving ? <Loader size={14} className="animate-spin" /> : <Save size={14} />} 저장</button></div></div></div></div>);
};

export default FinanceModal;
